#ifndef CONFIG_H
#define CONFIG_H

// ===================== 业务配置常量 =====================
#define MAX_DAILY_BORROW_NUM 5    // 每日最高可借阅量（核心配置）
#define STOCK_WARN_THRESHOLD 5    // 库存预警阈值（可选，复用之前的规则）
#define MAX_SINGLE_BORROW_NUM 5   // 单次单本图书最大借阅量（可选，补充规则）


#endif // CONFIG_H